/**
 * DeliveryPage.tsx — seshaa.delivery
 * Thin wrapper around RidePage with defaultType="DELIVERY"
 */
import RidePage from './RidePage';

export default function DeliveryPage() {
  return <RidePage defaultType="DELIVERY" />;
}
