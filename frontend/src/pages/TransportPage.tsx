/**
 * TransportPage.tsx — seshaa.transport
 * Thin wrapper around RidePage with defaultType="TRANSPORT"
 */
import RidePage from './RidePage';

export default function TransportPage() {
  return <RidePage defaultType="TRANSPORT" />;
}
